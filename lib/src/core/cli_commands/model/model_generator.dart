import '../../utils/log_utils.dart';

class ModelGenerator {
  static void generateModel(String onFeature, String jsonPath) {
    // Implement generating a model
    LogService.info('Generating model for feature: $onFeature from $jsonPath');

    // Add your code to generate the model from the given path
    // For example, you can read the JSON file, parse it, and generate Dart code.
  }
}
